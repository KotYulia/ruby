<%= form_for vendor do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>

  <%= f.hidden_field :supplier_id, :value => params["id"] %>

  <%= f.submit %>
<% end %>
<h1>Edit task</h1>

<%= form_for @task do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :text %>
    <%= f.text_area(:text, size: '50x10') %>
  </div>

  <%= f.submit %>
<% end %>

<%= link_to 'Back', user_path(@task.user.id) %>